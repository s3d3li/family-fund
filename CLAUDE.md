# مشروع صندوق العائلة

## الهدف
إعادة تنظيم وتطوير المشروع القديم تدريجيًا داخل Claude Code بدون فقد أي بيانات أو تغيير السلوك الحالي إلا بعد اختبار واعتماد كل مرحلة.

## الحالة الحالية
- React + Vite.
- التطبيق الحالي متركز بشكل رئيسي في App.jsx.
- قاعدة البيانات Supabase.
- الجدول الحالي: fund_data.
- السجل الرئيسي: id = "main".
- بيانات التطبيق موجودة داخل الحقل data كـ JSON.
- البيانات المؤكدة وقت بداية النقل:
  - members: 38
  - payments: 65
  - expenses: 2
- توجد نسخة احتياطية JSON خارج المشروع تم التحقق منها بهذه الأعداد.
- توجد نسخة أمان للكود على GitHub في الفرع:
  backup-before-claude-code-2026-09-17
- فرع العمل الحالي:
  migrate-to-claude-code
- نقطة الكود الأصلية قبل النقل:
  f156a81

## قواعد أمان غير قابلة للتجاوز
1. لا حذف أو استبدال أو تصفير بيانات Supabase.
2. لا migration لقاعدة البيانات بدون خطة مكتوبة ونسخة احتياطية واختبار رجوع.
3. قبل أي تعديل يمس التخزين، اعرض الخطة أولًا وانتظر الموافقة.
4. لا تطبع أو توثق القيم الحقيقية للمفاتيح أو الأسرار.
5. لا تضع service_role key داخل كود المتصفح.
6. لا تعمل force push أو reset --hard أو حذف فروع الأمان.
7. لا تعمل commit أو push إلا بعد عرض git diff ونتائج الاختبار والحصول على موافقة.
8. كل مرحلة تكون صغيرة وقابلة للرجوع.
9. الحفاظ على توافق البيانات الحالية أولوية أعلى من إعادة الهيكلة.
10. لا تغيّر شكل fund_data أو الحقل data في المراحل الأولى.
11. لا تغيّر أسماء members/payments/expenses أو بنيتها دون migration معتمد.
12. قبل كل مرحلة وبعدها يجب تشغيل git status ومقارنة الحالة.
13. إذا ظهر أي تعارض أو شك في سلامة البيانات، توقف ولا تحاول الإصلاح تلقائيًا.

## ترتيب التطوير
المرحلة 1: التوثيق والحماية.
المرحلة 2: نقل إعدادات Supabase إلى متغيرات بيئة بدون تغيير السلوك.
المرحلة 3: معالجة PIN وآلية الدخول بشكل آمن.
المرحلة 4: مراجعة RLS وStorage بشكل منفصل ومدروس.
المرحلة 5: تقسيم App.jsx تدريجيًا إلى مكونات مع الحفاظ على نفس الوظائف.
المرحلة 6: إضافة اختبارات أساسية.
المرحلة 7: دراسة تحسين نموذج البيانات، ولا تنفذ migration إلا بموافقة منفصلة.

## طريقة العمل
في كل خطوة:
PLAN -> BACKUP CHECK -> CHANGE -> TEST -> DIFF -> APPROVAL -> COMMIT

## الحالة الأمنية بعد المرحلة 3
- Supabase Auth مستخدم لتسجيل دخول المشرفين عبر Email + Password.
- التطبيق متاح للعامة للعرض بدون تسجيل دخول.
- public.admin_users موجود ويرتبط بـ auth.users.id.
- يوجد حاليًا حساب مشرف واحد معتمد، والنظام يدعم إضافة عدة مشرفين لاحقًا.
- RLS مفعّل على fund_data.
- القراءة العامة من fund_data مسموحة.
- INSERT / UPDATE / DELETE على fund_data للمشرفين المعتمدين فقط.
- سياسة public write القديمة تم حذفها.
- اختبار anon للكتابة أعاد 0 rows.
- اختبار المشرف للكتابة نجح داخل transaction مع rollback بدون تغيير البيانات.
- bucket receipts بقي public للقراءة.
- SELECT على receipts متاح للعامة.
- INSERT / UPDATE / DELETE على receipts للمشرفين المعتمدين فقط.
- سياسة receipts insert العامة القديمة تم حذفها.
- ADMIN_PIN وsessionStorage الخاص بالـPIN أزيلا بالكامل.
- عمليات الكتابة في التطبيق تتطلب accessToken لجلسة المشرف.
- العرض العام ودخول وخروج المشرف اختُبرا يدويًا ونجحا.
- مشكلة Polling والشاشة البيضاء تم إصلاحها.
- npm run build ناجح.
- بيانات fund_data وبنية members/payments/expenses لم تتغير.
- النسخة الاحتياطية الأصلية للبيانات قبل النقل كانت:
  members: 38
  payments: 65
  expenses: 2
